# KUSecondHand 머지 계획 (PLAN.md)

작성일: 2026-05-10
업데이트: 2026-05-11 (Phase 1~3 완료 상태 반영)
대상 브랜치: `merge/integration` (main에서 분기, 현재 main과 동일)
참고: [docs/branch-analysis.md](docs/branch-analysis.md)

> **현황 (2026-05-11 최신)**: Phase 1~4 완료. 교환학생 기능 개발 중 — 세탁 인증 전면 제거, 채팅 목록 정리, 등록 취소, 패키지 가격, incoming/outgoing 자동 판별, 검색 시스템(hybrid: pgvector+pg_trgm, RRF) 구현 완료. IVFFlat→HNSW 인덱스 교체 완료. 남은 작업: placeholder 14개 채우기.

---

## 0. 확정된 결정사항 (2026-05-10)

| # | 결정 | 적용 |
|---|---|---|
| 1 | `/products` URL은 main 유지. item-recommendation의 `/products`는 **`/products/recommendations`** 로 이전. | ✅ Phase 2에서 처리 완료 |
| 2 | `/products/[pid]`도 main 유지. item-recommendation의 [pid]는 **`/products/recommendations/[pid]`** 로 이전. | ✅ Phase 2에서 처리 완료 |
| 3 | **미들웨어 보류**. `src/proxy.ts`는 그대로 둔다. 팀원이 의도적으로 rename했을 수 있어 추가 확인 필요. **머지 작업 전체 종료 후 재논의**. | §5 D1 deferred |
| 4 | 브랜치별 schema 차이는 main의 schema.sql + 마이그레이션 파일로 흡수 (item-rec: `item_categories.category_id` FK / exchange-student: `essential_packages.name_*` 정합화) | ✅ 마이그레이션 004, 005 완료 |
| 5 | 머지 단위는 **기능(브랜치)별 PR 분리**. Phase 1/2/3 각각 별도 PR. | ✅ 커밋 8개 분리 완료 |

> Next.js 라우팅 메모: `app/(main)/products/recommendations/page.tsx` (정적 세그먼트)는 `app/(main)/products/[pid]/page.tsx` (동적 세그먼트)보다 우선 매칭됩니다. 따라서 `/products/recommendations` 진입 시 main의 `[pid]`가 잡지 않고 새 정적 라우트가 동작합니다.

---

## 1. 단계별 머지 계획 (개요)

### Phase 0 — 사전 준비 (코드 수정 없음)
- [x] 결정사항 합의
- [x] `merge/integration` 브랜치가 main과 동일 상태 (HEAD `648ee07...`)
- [x] `.env.local` 준비 (§4) — 테스트 계정 `test@korea.ac.kr` 생성 완료

### Phase 1 — bundle-recommendation-code 통합 (위험도 ★) ✅ 완료
상세 계획은 §2 참고. URL 충돌 0건, schema 변경 0건.
**커밋**: `6242b74 feat(merge): bundle-recommendation-code 통합`
- 사전 수정: `0676308` (tsx 누락), `85735ac` (TS 에러 10건)

### Phase 2 — item-recommendation 통합 (위험도 ★★) ✅ 완료
- [x] schema 마이그레이션 (`db/migrations/004_item_categories_category_fk.sql`) + `schema.sql` 본체 패치
- [x] `/products` → `/products/recommendations` 이전
- [x] `/products/[pid]` → `/products/recommendations/[pid]` 이전
- [x] 추천 결과 내부 링크 7곳 경로 치환
- [x] `(main)` 그룹 하위 배치
**커밋**: `6a25f35` (선행 schema) + `aae2d6c` (통합)

### Phase 3 — exchange-student 통합 (위험도 ★★★) ✅ 완료
- [x] schema 마이그레이션 (`db/migrations/005_essential_packages_country_code.sql`) + `schema.sql` 본체 패치
- [x] A1: `createServerSupabaseClient` → `createClient` (API 3개)
- [x] A2: `essential_packages.name` JSONB → `name_ko/name_en` (5 파일)
- [x] 브랜치 결함 정정 (TDZ, Suspense)
- [x] 빈 placeholder 14개 의도적 미통합 (§3 참고)
**커밋**: `1805200` (선행 schema) + `ec128bb` (결함 정정) + `11c2d78` (통합)

### Phase 4 — 마무리 🔲 진행 중
- [ ] `.env.example` 신규 작성
- [x] `npm run type-check` — 0 에러
- [x] `npm run build` — 48페이지 정적 생성, 신규 라우트 모두 등록
- [x] `npm run dev` — 에러 0건
- [⚠️] `npm run lint` — skip (Next.js 16에서 `next lint` 제거됨, D6 deferred)
- [ ] (결정 #3) 미들웨어 활성화 논의 — D1 deferred

---

## 2. Phase 1 상세 계획 — bundle-recommendation-code

> **✅ 실행 완료 (커밋 `6242b74`)**

### 2.1. 가져올 파일 whitelist (16개) — 전체 완료

| # | 출처 (origin/bundle-recommendation-code) | 대상 (merge/integration) | 비고 |
|---|---|---|---|
| 1 | `src/app/(features)/recommend/package/page.tsx` | `src/app/(main)/recommend/package/page.tsx` | route group을 `(main)`으로 이동 (헤더/네비 공유) |
| 2 | `src/app/(features)/recommend/package/_components/PackageRecommendationClient.tsx` | `src/app/(main)/recommend/package/_components/PackageRecommendationClient.tsx` | |
| 3 | `src/app/(features)/recommend/package/_components/HousingTypeSelector.tsx` | 동일 디렉토리 | |
| 4 | `src/app/(features)/recommend/package/_components/CategorySection.tsx` | 동일 디렉토리 | |
| 5 | `src/app/(features)/recommend/package/_components/ProductCard.tsx` | 동일 디렉토리 | |
| 6 | `src/app/(features)/recommend/package/_components/PackageResult.tsx` | 동일 디렉토리 | |
| 7 | `src/app/(features)/recommend/resale-price/page.tsx` | `src/app/(main)/recommend/resale-price/page.tsx` | |
| 8 | `src/app/(features)/recommend/resale-price/_components/ResalePriceClient.tsx` | 동일 디렉토리 | |
| 9 | `src/app/(features)/recommend/resale-price/_components/ResalePriceForm.tsx` | 동일 디렉토리 | |
| 10 | `src/app/(features)/recommend/resale-price/_components/ResalePriceResult.tsx` | 동일 디렉토리 | |
| 11 | `src/app/api/recommend/category-avg-price/route.ts` | 동일 경로 | admin client 호출 함수명 어댑팅 (§2.4) |
| 12 | `src/lib/recommend/packageQueries.ts` | 동일 경로 | |
| 13 | `src/lib/recommend/resalePriceQueries.ts` | 동일 경로 | |
| 14 | `src/lib/recommend/resalePriceCalculator.ts` | 동일 경로 | |
| 15 | `src/lib/recommend/semesterUtils.ts` | 동일 경로 | |
| 16 | `src/types/recommend.ts` | 동일 경로 | main에 없는 신규 파일 |

### 2.2. 보존 자료 (선택) — 문서/시드

| 출처 | 대상 | 처리 |
|---|---|---|
| `proc/spec/feature-package-recommendation.md` | `proc/spec/feature-package-recommendation.md` | ✅ 보존 |
| `proc/spec/feature-resale-price.md` | `proc/spec/feature-resale-price.md` | ✅ 보존 |
| `proc/spec/combine.md` | `proc/spec/combine.md` | ✅ 보존 |
| `proc/seed/02-sample-data.sql` | `proc/seed/02-sample-data.sql` | ✅ 보존 (DB 적용은 별도) |
| `proc/seed/test-seed.sql` | `proc/seed/test-seed.sql` | ✅ 보존 |

### 2.3. 명시적 제외 (skip)

| 출처 | 사유 |
|---|---|
| `.DS_Store`, `proc/.DS_Store`, `skills/.DS_Store` | macOS 시스템 파일 |
| `install.sh` | 브랜치 작업자 셋업 스크립트 |
| `skills/**` (3개 SKILL.md, .Rhistory) | 브랜치 작업자 도구 |
| `.env.local.example` | Phase 4에서 통합본 작성 |
| `src/app/{layout,page,globals.css}` | main 유지 |
| `src/app/login/**`, `src/app/signup/**` | main의 `(auth)/login`·`(auth)/register` 사용 |
| `src/app/auth/callback/route.ts` | main의 `auth/callback/page.tsx` 패턴 유지 |
| `src/lib/supabase/{client,server,admin}.ts` | main 유지 (main이 더 풍부) |
| `src/lib/providers.tsx` | main의 `src/components/Providers.tsx` 유지 |
| `package.json`, `package-lock.json`, `tsconfig.json`, `postcss.config.mjs`, `next-env.d.ts`, `.gitignore` | main 유지 |
| `schema.sql` | 정확히 동일 (0줄 차이) |
| `proc/seed/01-create-missing-tables.sql` | 테이블 이미 main schema.sql에 존재 (line 1822~) |

### 2.4. 충돌 예상 지점과 해결 방안

| # | 지점 | 충돌 종류 | 해결 |
|---|---|---|---|
| C1 | route group `(features)` vs `(main)` | UX 일관성 | ✅ `(main)/recommend/...` 로 이동. URL 동일. |
| C2 | `category-avg-price/route.ts`가 `createAdminClient` import | 함수명 불일치 | ✅ `createServiceRoleClient` + null 가드 적용 |
| C3 | bundle 코드의 `@/lib/providers` import | main에는 없음 | ✅ feature 파일들이 실제로 import하지 않음 — 충돌 없음 |
| C4 | bundle 페이지가 `if (!user) redirect('/login?next=...')` | main `/login` 표준과 호환 | ✅ LoginForm에 `next` 쿼리 처리 추가. 단, 추천 페이지가 next 쿼리를 붙여 보내지 않아 UX 소손 존재 (작은 이슈, 기능 영향 없음) |
| C5 | `package_template_items.categories(name)` 조인 RLS | RLS 정책 의존 | ✅ categories 전체 공개 읽기 — 안전 |
| C6 | `transactions.agreed_price`, `transactions.completed_at`, `transactions.status='completed'` 컬럼 | schema 컬럼명 일치 확인 | ✅ main schema에 존재 확인 |
| C7 | tsconfig path alias | 모든 import가 `@/...`만 사용 | ✅ 그대로 동작 |

### 2.5. 정확한 git 명령 시퀀스 (실행 완료)

```bash
git checkout origin/bundle-recommendation-code -- src/types/recommend.ts
git checkout origin/bundle-recommendation-code -- src/lib/recommend/
git checkout origin/bundle-recommendation-code -- src/app/api/recommend/
mkdir -p "src/app/(main)/recommend"
git checkout origin/bundle-recommendation-code -- "src/app/(features)/recommend/"
git mv "src/app/(features)/recommend/package"      "src/app/(main)/recommend/package"
git mv "src/app/(features)/recommend/resale-price" "src/app/(main)/recommend/resale-price"
rmdir "src/app/(features)/recommend" "src/app/(features)"
# C2 어댑팅 (Edit 도구)
# docs/seed 보존
git add -A
git commit -m "feat(merge): bundle-recommendation-code 통합"
```

### 2.6. 검증 결과

| 단계 | 명령 | 결과 |
|---|---|---|
| V1 | `npm install` | ✅ |
| V2 | `npm run type-check` | ✅ 0 에러 |
| V3 | `npm run lint` | ⚠️ skip (§9 참고) |
| V4 | `npm run build` | ✅ |
| V5 | `/recommend/package` 브라우저 검증 | ✅ 정상 렌더 |
| V6 | `/recommend/resale-price` 브라우저 검증 | ✅ 정상 렌더 |
| V7 | `/api/recommend/category-avg-price` curl | ✅ 503 graceful (env key 미설정 — §10 참고) |
| V8 | `git diff main...merge/integration --stat` | ✅ whitelist 파일만 추가 확인 |

---

## 3. Phase 2 — item-recommendation 통합 ✅ 완료

**커밋**: `6a25f35` (선행 schema) + `aae2d6c` (통합)

핵심 변경:
- **선행 마이그레이션**: `db/migrations/004_item_categories_category_fk.sql` 신설 + `schema.sql` line 1375~ 부근에 `category_id INTEGER NULL` 컬럼 + FK + 인덱스 + COMMENT 동시 패치.
- **URL 분담** (결정 #1, #2):
  - `app/products/page.tsx` → `app/(main)/products/recommendations/page.tsx`
  - `app/products/[pid]/page.tsx` → `app/(main)/products/recommendations/[pid]/page.tsx`
- **링크 일괄 수정**: 내부 링크 7곳 `/products?cat_id=...` → `/products/recommendations?cat_id=...` 치환 완료.
- 시드 파일 3개 보존: `proc/seed/item-recommendation/` (item_categories_mapping_dev.sql, products_dev.sql, recommendations_dev.sql)

---

## 4. 환경변수 체크리스트 (.env.local)

```
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=          # admin client / API routes (transactions 집계)
DEEPL_AUTH_KEY=                     # exchange-student 채팅 번역 (Phase 3)
```

`.env.example` — Phase 4에서 신규 작성 예정 (미완료).

---

## 5. 머지 작업 종료 후 별도 논의 (deferred)

| # | 항목 | 사유 |
|---|---|---|
| D1 | **`src/proxy.ts` 정상화 여부** (결정 #3) | 팀원이 `middleware` → `proxy`로 의도적으로 rename했을 가능성. 의도 확인 필요. 현재 dead code 상태 — 인증 가드 미작동. 머지 완료 후 팀 합의 후 (a) `middleware.ts`로 rename 활성화, (b) 의도 보존 + 별도 활성 메커니즘, (c) 영구 비활성 중 결정. |
| D2 | 매물 페이지 분담 후 UX 흐름 (메인 `/products` ↔ 추천 결과 `/products/recommendations`) | 머지 완료 후 실제 데이터로 사용자 흐름 검증. |
| D3 | i18next 초기화 정착 (Phase 3 이후) | namespace JSON, locale switcher 등 미구현 — 채팅 번역 외 i18n 활용 결정. |
| D4 | bundle의 `(main)/recommend/*` 진입을 `(main)/layout.tsx` 네비에 노출할지 | UX 결정. |
| D5 | 시드 SQL 적용 정책 | dev/staging 별 적용 시점, owner 합의. |
| D6 | ESLint flat config 추가 (Next.js 16 대응) | `eslint.config.mjs` 신규 작성 + `package.json` lint 스크립트를 `"eslint ."` 으로 변경. 별도 PR. |

---

## 6. 위험 요인 & 미지수

| 위험 | 영향 | 완화 |
|---|---|---|
| `essential_packages.name` JSONB→`name_ko/name_en` 변경 영향 범위가 예상보다 넓을 수 있음 | exchange-student 코드 수정 증가 | ✅ grep으로 5 파일 식별 후 일괄 어댑팅 완료 |
| main의 `lib/auth/*`, OTP 흐름과 브랜치들의 자체 로그인 가정 불일치 | UX 어색함 | ✅ 브랜치 자체 LoginForm 모두 폐기, main 표준 사용 |
| `/login?next=...` 쿼리 처리가 main에 없을 수 있음 | 추천 페이지 → 로그인 → 복귀 흐름 끊김 | ⚠️ LoginForm에 처리 추가했으나 추천 페이지가 next 쿼리를 붙여 보내지 않아 소손 잔존. 추후 별도 처리. |
| Supabase RLS 정책이 service_role 가정인데 anon으로 호출되는 경우 | 데이터 못 읽음 | ✅ API route 항상 createServiceRoleClient 사용. |

---

## 7. 머지 작업의 git 패턴

각 파일을 가져올 때 사용한 명령:

```bash
# 단일 파일
git checkout origin/<branch> -- <path>

# 디렉토리 통째
git checkout origin/<branch> -- <path>/

# 가져온 후 ls + git status로 확인
git status
git diff --stat HEAD
```

> `git merge --allow-unrelated-histories` 는 사용 금지. orphan 히스토리 4개라 충돌이 거의 모든 파일에서 발생.

---

## 8. 현황 (2026-05-11 기준)

| 단계 | 상태 |
|---|---|
| Phase 1 — bundle-recommendation-code | ✅ 완료 (`6242b74`) |
| Phase 2 — item-recommendation | ✅ 완료 (`aae2d6c`) |
| Phase 3 — exchange-student | ✅ 완료 (`11c2d78`) |
| Phase 4 — 마무리 | ✅ `.env.example` 생성 완료 |
| PR 제출 | 🔲 대기 중 (머지 마무리 후) |
| DB 마이그레이션 003~009 적용 | ✅ **kusecond-test** 프로젝트에만 적용 완료. **kusecond-set 프로젝트에는 미적용** — 인수인계 후 003→009 순서대로 Supabase SQL Editor에서 실행 필요. |
| 검색 시스템 설계 | ✅ `docs/search-design.md` 작성 완료 |
| 검색 시스템 개발 | ✅ 구현 완료. openai SDK 설치. embeddings.ts + hybridSearch.ts + /api/search/products + /api/search/packages + /search 페이지(일반 유저: 매물만, 교환학생: 매물+패키지 탭). migration 008 Supabase 적용 완료. |
| 네비게이션 통합 | ✅ (main) 상단 네비에 교환학생 링크 추가, (exchange) 하단→상단 네비 통일 |
| 마이페이지 통합 | ✅ /my/profile 단일화 — ProfileForm(일반) + ExchangeProfileSection(항상 표시: 프로필 없으면 신규 등록, 있으면 편집. 국가·거주형태·학기·역할·선호언어) + SignOutButton |
| 전체 플로우 설계 | ✅ `docs/flow-design.md` 작성 완료 |
| 레이아웃 단일화 | ✅ (exchange) route group 제거, 모든 route (main)으로 통합, NavBar 검색창·상품 등록 추가 |
| /packages 웹 UI | ✅ ← 홈 제거, 개발 미리보기 배너 제거, 셀러 카드 그리드, 셀러 등록 버튼 추가 |
| /chat 웹 UI | ✅ ← 홈·개발 배너 제거, 로그인 안내 화면, 상태 뱃지, 웹 스타일 리스트 |
| /wishlist 웹 UI | ✅ ← 홈·개발 배너 제거, 로그인 안내 화면, 웹 스타일 리스트 |
| 한국 국가 추가 | ✅ schema.sql 시드 추가 (KR) — Supabase SQL Editor에서 INSERT 실행 필요 |
| 패키지 추천 웹 UI | ✅ <main> 중복 제거, sticky 플로팅 바→인라인, 가로스크롤→그리드, 고정 카드 폭 제거 |
| 온보딩 구현 | 🔲 미시작 (flow-design.md §3 참고) |
| 일반 상품 등록 | ✅ /products/new — 탭 제거(일반 상품 폼만), 이미지 업로드, 카테고리 계층 선택, POST /api/products 구현 |
| /sell/* 웹 UI 통일 | ✅ template·register 2단계 탭(일반 상품/패키지)·max-w-2xl·rounded-xl 카드·indigo 버튼 스타일 통일. 세탁 인증(3단계) 제거 |
| 패키지 게시 오류 수정 | ✅ buyer_uid NOT NULL 제약 위반 → migration 006 작성. 임시로 오류 메시지 명확화, 마이그레이션 적용 후 buyer_uid=null로 수정 예정 |
| 패키지 상품 시드 데이터 | ✅ proc/seed/04-package-seed.sql — essential_packages 3종 + package_items |
| 일반 상품 위시리스트 | ✅ GET/POST /api/wishlists, DELETE /api/wishlists/[pid], 상품 상세 찜하기 버튼, /wishlist 탭 UI (상품/패키지 통일 카드) |
| 교환학생 시드 데이터 | ✅ proc/seed/03-exchange-seed.sql — 4개 테스트 계정(US/DE/JP/KR) |
| 세탁 인증 전면 제거 | ✅ score.ts·types/package.ts·packages/page.tsx·packages/[id]/page.tsx·sell/register/page.tsx에서 hygiene 코드 완전 제거. sell/hygiene/page.tsx는 /sell/template로 redirect. |
| 채팅 목록 정리 | ✅ 빈 채팅 목록 "패키지 추천 보기" 링크 제거 |
| 등록 취소 기능 | ✅ packages/[id] 셀러 뷰 — 바이어 없을 때 "등록 취소하기" 버튼 추가 (status='cancelled' 업데이트 후 /packages 이동) |
| 패키지 가격 책정 | ✅ migration 007 Supabase 적용 완료. sell/register 가격 입력 필드(콤마 포맷, 선택). packages 셀러 카드 + packages/[id] 헤더에 가격 표시. |
| incoming/outgoing 역할 선택 제거 | ✅ ExchangeProfileSection에서 역할 드롭다운 제거. 국가 코드 KR → incoming, 그 외 → outgoing 자동 결정. |
| 검색 시스템 | ✅ pgvector semantic + pg_trgm fuzzy, RRF(k=60) 융합. Migration 008 적용. openai text-embedding-3-small. /api/search/products + /api/search/packages + /search 페이지. 일반 유저: 매물만, 교환학생: 매물+패키지 탭. |
| 기존 데이터 embedding backfill | ✅ scripts/backfill-embeddings.ts 실행 완료 — 매물 39개, 패키지 3개 embedding 생성. |
| IVFFlat → HNSW 인덱스 교체 | ✅ lists=100인데 rows=39라 IVFFlat이 빈 클러스터만 스캔하는 문제 발견. Supabase SQL Editor에서 DROP + HNSW 재생성 완료. |
| 검색 테스트 | ✅ 정상 동작 확인. "노트북" → 델 XPS, 레노버 씽크패드 상위 노출. 수정 사항: ①IVFFlat→HNSW 교체(Supabase SQL) ②search API route를 anon→service role 클라이언트로 변경 ③embedding string 형식([x,x,...])으로 RPC 전달 |
| placeholder 14개 채우기 | 🔲 리팩토링 성격 — 기능은 이미 인라인으로 동작 중. 우선순위 낮음 |
| 일반 상품 채팅 | ✅ ChatButton 컴포넌트, products/[pid] 채팅하기 버튼, chat/send match_id 선택값으로, chat/[id] product/package 분기, chat/page 상품채팅 목록 포함. chat/send cp_id 버그 수정. |

### 긴급: DB 마이그레이션 미적용 이슈

`/products` 페이지에서 `column users_1.successful_trade_count does not exist` 오류 발생.
migration 003~005가 Supabase에 적용되지 않아 발생. 아래 순서로 SQL Editor에서 실행 필요:

```
1. db/migrations/003_trust_successful_trade_count.sql
2. db/migrations/004_item_categories_category_fk.sql
3. db/migrations/005_essential_packages_country_code.sql
```

세 파일 모두 `ADD COLUMN IF NOT EXISTS` — 기존 데이터 영향 없음, 안전.

---

## 9. V3 lint — pre-existing main breakage (deferred → D6)

> 기록일: 2026-05-10 | Phase 1 검증 중 발견. cherry-pick과 무관.

### 원인 요약

| 항목 | 상태 |
|------|------|
| Next.js 버전 | 16.2.6 |
| `next lint` subcommand | **제거됨** (Next 15+에서 deprecated, 16에서 삭제) |
| `package.json` `"lint"` 스크립트 | `"next lint"` — main 업그레이드 시점부터 **broken** |
| ESLint 설정 파일 | **부재** (`.eslintrc.*`, `eslint.config.*` 모두 없음) |
| ESLint 패키지 | eslint ^9.25.1, eslint-config-next: latest, @typescript-eslint/* ^8.30.1 설치됨 |

### 실제 실행 결과 (2026-05-10)

```
$ npm run lint
> next lint
Invalid project directory provided, no such directory: .../lint
---EXIT: 0   ← 오해 소지 (lint 자체가 실행되지 않음)

$ npx eslint .
Oops! Something went wrong! :(
ESLint: 9.39.4
ESLint couldn't find an eslint.config.(js|mjs|cjs) file.
From ESLint v9.0.0, the default configuration file is now eslint.config.js.
---EXIT: 0
```

### 조치 방향 (D6 — 별도 PR)

- cherry-pick 파일과 **무관** — Phase 1 통합 자체의 회귀 없음
- 별도 이슈로 분리: `eslint.config.mjs` 신규 작성 + `package.json` lint 스크립트를 `"eslint ."` 으로 변경

---

## 10. V7 — 환경 의존 검증 (skipped with note)

> 기록일: 2026-05-10 | Phase 1 검증 중 확인.

### 실행 결과

```
GET /api/recommend/category-avg-price?category_id=1
→ HTTP 503  {"error": "service unavailable"}
```

### 원인

`.env.local`에 `SUPABASE_SERVICE_ROLE_KEY` 미설정.
`createServiceRoleClient()`가 `null` 반환 → route.ts의 null 가드가 503 응답.

### 검증된 것 ✅

- `/api/recommend/category-avg-price` 빌드에 포함됨 (V4 통과)
- 라우트 등록됨 (503 응답 = 라우트에 도달함, 404 아님)
- C2 어댑팅에서 추가한 null 가드가 의도대로 동작 (크래시 없이 503 graceful 응답)
- 코드 자체 정상

### 미검증 ⏳

- 실제 DB 쿼리 응답 (`avg_price`, `count` 필드)
- `SUPABASE_SERVICE_ROLE_KEY` 설정 후 별도 확인 필요

---

## 11. V5/V6 — 브라우저 검증 완료

> 기록일: 2026-05-10 작성 → 2026-05-11 완료로 업데이트

| 단계 | URL | 상태 |
|------|-----|------|
| V5 | `http://localhost:3000/recommend/package` | ✅ 정상 렌더 |
| V6 | `http://localhost:3000/recommend/resale-price` | ✅ 정상 렌더 |
| — | Phase 2 `/recommendations`, `/products/recommendations` | ✅ 정상 렌더 |
| — | Phase 3 `/packages`, `/sell/*`, `/wishlist`, `/mypage`, `/chat` | ✅ 정상 렌더 |

테스트 계정: `test@korea.ac.kr` (Supabase에서 직접 생성)
DB 시드 미적용 상태 — 데이터 노출 검증은 시드 적용 후 별도 진행 가능.

---

## 12. Phase 3 — exchange-student 상세 (추가 기록)

> Phase 3는 PLAN.md 작성 시점에 상세 계획이 없었으므로 실제 결과를 이 섹션에 기록.

**통합 커밋**: `1805200` (선행 schema) + `ec128bb` (결함 정정) + `11c2d78` (통합)

**통합된 라우트** (`(exchange)` route group)
- `/packages`, `/packages/[id]`
- `/sell`, `/sell/template`, `/sell/register`, `/sell/hygiene`
- `/wishlist`
- `/mypage`
- `/chat`, `/chat/[id]`

**통합된 API**
- `/api/chat/send`
- `/api/matches/auto`
- `/api/matches/complete`

**통합된 파일**
- `src/types/` — exchange.ts, match.ts, package.ts, supabase.ts
- `src/hooks/useExchangeProfile.ts`
- `src/constants/schoolDomains.ts`
- `src/lib/deepl/translate.ts`
- `src/lib/utils/` — fallback.ts, format.ts, score.ts
- `docs/exchange_student_*.md` 7개

**브랜치 결함 정정** (`ec128bb`)
- `chat/[id]/page.tsx`: const TDZ 위반 → 변수 선언 위치 이동
- `sell/register/page.tsx`: `useSearchParams` Suspense 미분리 → Suspense wrapper 적용

**의도적 미통합 — 빈 placeholder 14개**
- components (9): ChatBubble, ChatInput, TranslationToggle, HygieneBadge, MatchStatusBadge, PackageCard, PackageItemList, SellerCard, SupplementGuide
- hooks (3): useChat, useMatches, usePackages
- constants (2): packages, regions
- 모두 0 byte, import 0건 dead code — 후속 PR로 채울 수 있음 (exchange-student 담당자와 논의)
